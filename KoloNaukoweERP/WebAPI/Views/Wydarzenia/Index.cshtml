@model IEnumerable<DAL.Entities.Wydarzenie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Zespol)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Miejsce)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Zespol.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Miejsce)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdWydarzenia">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdWydarzenia">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdWydarzenia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
